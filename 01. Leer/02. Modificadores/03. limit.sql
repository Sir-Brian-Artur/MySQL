-- Limitamos la cantidad de resultados que necesitamos
SELECT * FROM hello_mysql.users LIMIT 2;
SELECT * FROM hello_mysql.users LIMIT 3;

/* Es algo muy significativo cuando tenemos una base
de datos uy grande y tan sólo necesitamos unos cuantos 
resultados de forma rápida. */