SELECT * FROM users;                /*Con SELECT recuperamos los valores de */
                                    /*las columnas/atributos que hayamos    */
SELECT name FROM users;             /*seleccionado, en la tabla especificada*/

SELECT user_id, name FROM users;