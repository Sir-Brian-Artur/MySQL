/* "COUNT" devuelve el *NÚMERO* de filas que coinciden 
con un criterio específicado. */
SELECT COUNT(age) FROM users;

/* Con "*" le estamos pidiendo que nos muestre el número
de registros que tiene la tabla. */
SELECT COUNT(*) FROM users;