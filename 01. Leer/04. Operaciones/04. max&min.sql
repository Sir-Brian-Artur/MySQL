/* Lo empleamos para optener sólamente el valor más
alto de nuestra vase de datos. Sin nigún dato a mayores
de ese mismo registro. */
SELECT MAX(age) FROM users;

-- Valor mínimo
SELECT MIN(age) FROM users;